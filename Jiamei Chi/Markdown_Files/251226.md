# 251226 README

## 微调

微调后的模型生成代码（60%）存在语法错误，主要包括

- 缩进混乱，混用了 `Tab` 和 `Space`
- 未输出正确的转义字符，如 `\` 需要转义为 `\\`
- 结构类型错误：如列表（`- type: word`）生成错误

已经检查过训练集（812条）并不存在任何语法问题，现在使用 `nuclei -validate` 自动检查生成的候选 YAML，利用惩罚机制监督模型微调

## 路径侦察

agent在推理**特定的 API 端点**或**非标准目录结构**时，容易产生“幻觉”，倾向于套用该软件最知名的攻击路径而非该 CVE 对应的实际路径。

```
CVE-2017-7494
Generated：/IPC$
Ground Truth：/myshare

CVE-2018-2894
Generated：/wls-wsat/*
Ground Truth：/ws_utc/config.do

CVE-2023-26360 (Adobe ColdFusion)
Generated：/CFIDE/administrator
Ground Truth：/cf_scripts/scripts/ajax/ckeditor/plugins/filemanager/iedit.cfc
```
